<!DOCTYPE html>
<html>
<head>
    <title>FREQUÊNCIA APAE</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="{% url 'student_list' %}">FREQUÊNCIA APAE</a>
            <div class="navbar-nav">
                <a class="nav-link" href="{% url 'student_list' %}">Cadastro</a>
                <a class="nav-link" href="{% url 'attendance_register' %}">Frequência</a>
                <a class="nav-link" href="{% url 'generate_report' %}">Relatórios</a>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
        {% if messages %}
            {% for message in messages %}
                <div class="alert alert-{{ message.tags }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endif %}
        
        {% block content %}
        {% endblock %}
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
